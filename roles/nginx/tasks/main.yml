- name: Install nginx
  package:
    name: nginx
    state: present

- name: Deploy website
  template:
    src: index.html.j2
    dest: "{{ web_root }}/index.html"

- name: Start nginx
  service:
    name: nginx
    state: started
    enabled: yes
